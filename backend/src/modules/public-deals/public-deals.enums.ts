export enum PublicLockStatus {
  ACTIVE = "active",
  EXPIRED = "expired",
  CONVERTED = "converted",
}

export enum PublicBookingStatus {
  PENDING = "pending",
  PAID = "paid",
  FAILED = "failed",
}

export enum VariantSlotStatus {
  AVAILABLE = "available",
  BOOKED = "booked",
  LOCKED = "locked",
  CANCELLED = "cancelled",
}
