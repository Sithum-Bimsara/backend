export enum MerchantVerificationStatus {
  PENDING = "pending",
  VERIFIED = "verified",
}

export enum DealRequestStatus {
  NEW = "new",
  CONTACTED = "contacted",
  CLOSED = "closed",
}

export enum UserStatus {
  ACTIVE = "active",
  SUSPENDED = "suspended",
}

export enum AdminDealStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
}
